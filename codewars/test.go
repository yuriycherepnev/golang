package main

import "fmt"

func main() {
	dx, dy := 1, 0
	fmt.Println(dx, dy)
	dx, dy = -dy, dx
	fmt.Println(dx, dy)
	dx, dy = -dy, dx
	fmt.Println(dx, dy)
	dx, dy = -dy, dx
	fmt.Println(dx, dy)
	dx, dy = -dy, dx
	fmt.Println(dx, dy)
}
